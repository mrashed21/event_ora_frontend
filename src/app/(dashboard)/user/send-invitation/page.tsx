import SendInvaitation from "@/components/user/send-invaitation/send-invaitation";

export const metadata = {
  title: "Send Invitation",
  description: "This is the send invitation page.",
};
const SendInvitationPage = () => {
  return (
    <section>
      <SendInvaitation />
    </section>
  );
};

export default SendInvitationPage;
